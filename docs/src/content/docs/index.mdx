---
title: InfraAdvisor AI
description: AI-powered infrastructure advisory platform for AEC/O&M consulting firms
template: splash
hero:
  tagline: Infrastructure advisory at your fingertips. Multi-agent AI for AEC/O&M consulting firms.
  actions:
    - text: Quickstart
      link: /infra-advisor-ai/deployment/quickstart/
      icon: right-arrow
    - text: Architecture
      link: /infra-advisor-ai/architecture/
      icon: open-book
      variant: minimal
---

import { Card, CardGrid, LinkCard } from '@astrojs/starlight/components';
import { ContainerSection } from 'lucode-starlight/components';
import Mermaid from '../../components/Mermaid.astro';

<ContainerSection>

InfraAdvisor AI lets infrastructure consultants ask natural-language questions against real government datasets — bridge conditions, disaster history, energy capacity, water infrastructure, and federal procurement — and receive cited, synthesized answers in seconds.

<CardGrid>
  <Card title="Multi-agent reasoning" icon="rocket">
    A router LLM routes each query to the right domain specialist. Each specialist works with a curated tool subset to reduce hallucination surface.
  </Card>
  <Card title="Live government data" icon="open-book">
    9 Airflow pipelines ingest 615k+ records weekly from FHWA, FEMA, EIA, EPA, and TWDB into a hybrid vector + BM25 search index.
  </Card>
  <Card title="Full-stack observability" icon="magnifier">
    Every layer instrumented with Datadog — APM, LLM Observability, RUM session replay, Data Streams, Data Jobs, and CSPM.
  </Card>
  <Card title="Dual implementations" icon="setting">
    Python (FastAPI + LangChain + ddtrace) and .NET (ASP.NET Core 10 + OpenTelemetry) stacks run side-by-side. Switch between them in the UI.
  </Card>
</CardGrid>

</ContainerSection>

<ContainerSection>

## Architecture at a glance

<Mermaid code={`
flowchart TD
    Browser["Browser\\nReact + Datadog RUM"]

    subgraph k8s["AKS · infra-advisor namespace"]
        Auth["Auth API\\nFastAPI + PostgreSQL + JWT"]
        AgentPy["Agent API (Python)\\nLangChain ReAct + LangGraph"]
        AgentDN["Agent API (.NET)\\nASP.NET Core 10 + OTel"]
        MCPPy["MCP Server (Python)\\n11 tools over MCP HTTP"]
        MCPNet["MCP Server (.NET)\\n11 tools · .NET port"]
        Redis["Redis\\nSession memory · 24h TTL"]
        Kafka["Kafka (Strimzi)\\ninfra.query.events"]
    end

    subgraph azure["Azure"]
        Airflow["Airflow · 9 DAGs\\nFHWA · FEMA · EIA · TWDB · Synthetic"]
        Blob["Blob Storage\\nParquet · date-partitioned"]
        Search["AI Search\\n615k+ docs · hybrid BM25 + vector"]
    end

    DD["Datadog Agent\\nAPM · LLM Obs · RUM · DJM · DSM · CSPM"]

    Browser --> Auth
    Browser --> AgentPy & AgentDN
    AgentPy --> MCPPy & Redis & Kafka
    AgentDN --> MCPNet & Redis & Kafka
    MCPPy & MCPNet --> Search
    Airflow --> Blob --> Search
    k8s --> DD
`} />

</ContainerSection>

<ContainerSection>

## Explore the docs

<CardGrid>
  <LinkCard title="Architecture" href="/infra-advisor-ai/architecture/" description="System design, service map, and data flow" />
  <LinkCard title="Services" href="/infra-advisor-ai/services/" description="API reference for each microservice" />
  <LinkCard title="Data Pipeline" href="/infra-advisor-ai/data-pipeline/" description="Airflow DAGs and ingestion details" />
  <LinkCard title="Observability" href="/infra-advisor-ai/observability/" description="Datadog instrumentation across every layer" />
  <LinkCard title="Deployment" href="/infra-advisor-ai/deployment/" description="Prerequisites, secrets, step-by-step deploy" />
  <LinkCard title="Development" href="/infra-advisor-ai/development/" description="Local setup, testing, conventions" />
</CardGrid>

</ContainerSection>

<ContainerSection>

## Technology stack

| Layer | Technology |
|---|---|
| Cloud | Azure (AKS, OpenAI, AI Search, Blob Storage) |
| Orchestration | Kubernetes 1.30 on AKS — 3× Standard_D2s_v3 nodes |
| LLM inference | Azure OpenAI (gpt-4.1-mini, gpt-4.1) |
| Embeddings | Azure OpenAI text-embedding-3-small |
| Knowledge base | Azure AI Search (hybrid vector + BM25) |
| Agent framework | LangChain ReAct + LangGraph |
| Tool protocol | Model Context Protocol (MCP) over HTTP |
| Session memory | Redis (K8s Deployment) |
| Data pipeline | Apache Airflow 3.x (LocalExecutor, Helm chart) |
| Message bus | Apache Kafka (Strimzi Operator on AKS) |
| Observability | Datadog (APM, LLM Obs, RUM, DJM, DSM, CSPM) |
| Auth | FastAPI + PostgreSQL + JWT + bcrypt |
| Frontend | React 18 + Chakra UI v3 + Vite |
| IaC | Azure Bicep + Helm + kubectl manifests |
| CI/CD | GitHub Actions → GHCR → AKS rolling deploy |

</ContainerSection>

<ContainerSection>

## Live environment

| Resource | URL |
|---|---|
| Application | `https://infra-advisor-ai.kyletaylor.dev` |
| Container registry | `ghcr.io/kyletaylored/infra-advisor-ai` |

The Airflow UI and Mailpit inbox are intentionally not listed here.
Both sit at protected paths (`/airflow`, `/mailpit`) with credentials required, and you should put Cloudflare Access in front of each for defense-in-depth before sharing the URLs.

</ContainerSection>
