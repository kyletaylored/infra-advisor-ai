apiVersion: v1
kind: Service
metadata:
  name: mcp-server-dotnet
  namespace: infra-advisor
  labels:
    app: mcp-server-dotnet
spec:
  type: ClusterIP
  selector:
    app: mcp-server-dotnet
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
