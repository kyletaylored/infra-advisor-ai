apiVersion: v1
kind: Service
metadata:
  name: ui
  namespace: infra-advisor
  labels:
    app: ui
spec:
  type: ClusterIP
  selector:
    app: ui
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
