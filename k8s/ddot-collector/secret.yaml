# TEMPLATE FILE — contains placeholder values only.
# Do NOT put real credentials here. This file is committed to the repo.
#
# Set DD_API_KEY in your environment and apply with:
#   kubectl create secret generic datadog-agent-secret \
#     --from-literal=DD_API_KEY="${DD_API_KEY}" \
#     -n datadog --dry-run=client -o yaml | kubectl apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: datadog-agent-secret
  namespace: datadog
type: Opaque
stringData:
  DD_API_KEY: "REPLACE_ME"
