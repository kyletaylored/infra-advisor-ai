# TEMPLATE FILE — contains placeholder values only.
# Do NOT put real credentials here. This file is committed to the repo.
#
# To create the real secret at deploy time, run:
#   make create-redis-secret
# with the following var set in your environment (or .env):
#   REDIS_PASSWORD — used by redis-server --requirepass and by app services
apiVersion: v1
kind: Secret
metadata:
  name: redis-secret
  namespace: infra-advisor
type: Opaque
stringData:
  REDIS_PASSWORD: PLACEHOLDER
