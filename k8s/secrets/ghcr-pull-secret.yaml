# TEMPLATE FILE — contains placeholder values only.
# Do NOT put real credentials here. This file is committed to the repo.
#
# To create the real secret at deploy time, run:
#   make create-ghcr-secret
# with GHCR_PAT and GITHUB_EMAIL set in your environment.
#
# The Makefile target runs:
#   kubectl create secret docker-registry ghcr-pull-secret \
#     --namespace infra-advisor \
#     --docker-server=ghcr.io \
#     --docker-username=kyletaylored \
#     --docker-password=$(GHCR_PAT) \
#     --docker-email=$(GITHUB_EMAIL) \
#     --dry-run=client -o yaml | kubectl apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: ghcr-pull-secret
  namespace: infra-advisor
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: PLACEHOLDER_BASE64_DOCKER_CONFIG
