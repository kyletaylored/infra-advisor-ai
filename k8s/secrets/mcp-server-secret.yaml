# TEMPLATE FILE — contains placeholder values only.
# Do NOT put real credentials here. This file is committed to the repo.
#
# To create the real secret at deploy time, run:
#   make create-mcp-server-secret
# with the following vars set in your environment (or .env):
#   AZURE_SEARCH_ENDPOINT, AZURE_SEARCH_API_KEY,
#   AZURE_OPENAI_ENDPOINT, AZURE_OPENAI_API_KEY,
#   EIA_API_KEY, ERCOT_API_KEY
#
# EIA_API_KEY  — https://www.eia.gov/opendata/register.php (free)
# ERCOT_API_KEY — https://developer.ercot.com (requires ERCOT account)
apiVersion: v1
kind: Secret
metadata:
  name: mcp-server-secret
  namespace: infra-advisor
type: Opaque
stringData:
  AZURE_SEARCH_ENDPOINT: PLACEHOLDER
  AZURE_SEARCH_API_KEY: PLACEHOLDER
  AZURE_OPENAI_ENDPOINT: PLACEHOLDER
  AZURE_OPENAI_API_KEY: PLACEHOLDER
  EIA_API_KEY: PLACEHOLDER
  ERCOT_API_KEY: PLACEHOLDER
