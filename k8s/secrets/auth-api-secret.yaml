# TEMPLATE FILE — contains placeholder values only.
# Run: make create-auth-api-secret
# with DATABASE_URL and JWT_SECRET set in env
apiVersion: v1
kind: Secret
metadata:
  name: auth-api-secret
  namespace: infra-advisor
type: Opaque
stringData:
  DATABASE_URL: PLACEHOLDER
  JWT_SECRET: PLACEHOLDER
