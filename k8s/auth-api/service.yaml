apiVersion: v1
kind: Service
metadata:
  name: auth-api
  namespace: infra-advisor
  labels:
    app: auth-api
spec:
  type: ClusterIP
  selector:
    app: auth-api
  ports:
    - name: http
      port: 8002
      targetPort: 8002
      protocol: TCP
