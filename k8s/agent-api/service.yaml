apiVersion: v1
kind: Service
metadata:
  name: agent-api
  namespace: infra-advisor
  labels:
    app: agent-api
spec:
  type: ClusterIP
  selector:
    app: agent-api
  ports:
    - name: http
      port: 8001
      targetPort: 8001
      protocol: TCP
